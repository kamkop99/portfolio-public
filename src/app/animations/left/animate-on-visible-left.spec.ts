import { AnimateOnVisibleLeft } from './animate-on-visible-left';

describe('AnimateOnVisibleLeft', () => {
  it('should create an instance', () => {
    const directive = new AnimateOnVisibleLeft();
    expect(directive).toBeTruthy();
  });
});
