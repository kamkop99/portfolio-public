import { BaseAnimateOnVisible } from './base-animate-on-visible';

describe('BaseAnimateOnVisible', () => {
  it('should create an instance', () => {
    const directive = new BaseAnimateOnVisible();
    expect(directive).toBeTruthy();
  });
});
