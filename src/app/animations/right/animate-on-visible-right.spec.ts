import { AnimateOnVisibleRight } from './animate-on-visible-right';

describe('AnimateOnVisibleRight', () => {
  it('should create an instance', () => {
    const directive = new AnimateOnVisibleRight();
    expect(directive).toBeTruthy();
  });
});
