import { AnimateOnVisibleSideDirective } from './animate-on-visible-side.directive';

describe('AnimateOnVisibleSideDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateOnVisibleSideDirective();
    expect(directive).toBeTruthy();
  });
});
