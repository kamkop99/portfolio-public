import { AnimateOnVisibleDown } from './animate-on-visible-down.directive';

describe('AnimateOnVisibleDown', () => {
  it('should create an instance', () => {
    const directive = new AnimateOnVisibleDown();
    expect(directive).toBeTruthy();
  });
});
